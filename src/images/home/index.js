import { homeFeatureImages } from "./feature";
import { homeIntroImages } from "./intro";
import { homeProcessImages } from "./process";
import { homeResultImages } from "./result";
import { homeSliderImages } from "./slider";

export const homeImages = {
  feature: homeFeatureImages,
  intro: homeIntroImages,
  process: homeProcessImages,
  result: homeResultImages,
  slider: homeSliderImages,
};
