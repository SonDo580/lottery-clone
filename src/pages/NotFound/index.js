function NotFound() {
  return (
    <div
      style={{
        height: "40vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h1 style={{ fontSize: "3rem" }}>Page Not Found</h1>
    </div>
  );
}

export default NotFound;
